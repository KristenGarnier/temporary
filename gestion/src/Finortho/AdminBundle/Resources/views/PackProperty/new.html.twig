{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PackProperty creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('packproperty') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
