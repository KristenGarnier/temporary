{% extends 'FinorthoAdminBundle:Interface:index.html.twig' %}

{% block title %}
    Exigence {{ entity.name }}
{% endblock %}

{% block content -%}
    <h4>L'exigence {{ entity.name }}</h4>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        </tbody>
    </table>

    <div id="actions" class="">
        <a href="{{ path('packproperty') }}">
            <button class="btn cancel">
                Retout à la liste
            </button>

        </a>
        <a href="{{ path('packproperty_edit', { 'id': entity.id }) }}">
            <button class="btn cancel">
                Éditer
            </button>
        </a>
        {{ form(delete_form) }}
    </div>
{% endblock %}
